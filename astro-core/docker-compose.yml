version: "3.9"

services:
  astro-core:
    build: .
    container_name: astro-core
    ports:
      - "8000:8000"
    volumes:
      - ./ephemeris:/app/ephemeris:ro
    environment:
      - EPHE_PATH=/app/ephemeris
    restart: unless-stopped
